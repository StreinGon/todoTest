const LOGIN_CORRECT = 'Log In correct';

const LOGIN_INCORRECT = 'Log In incorrect';

const REDISTRATION_SUCCESSFULL = 'Registation successfull';

const LOGOUT = 'Log Out';

const LOGIN_USER = 'Log in as user';

const LOGIN_ADMIN = 'Log in as admin';

const AUTH_COOKIES = 'Authorization';

const UNAUTHORIZED = 'Unauthorized';

const TODO_CREATED = 'Todo created';

const TODO_UPDATED = 'Todo updated';

const TODO_DELETED = 'Todo deleted';

const TODO_SENDED = 'Todo sended';

const NOT_FOUND = 'Not found';

export {
  LOGIN_CORRECT,
  LOGIN_INCORRECT,
  REDISTRATION_SUCCESSFULL,
  LOGOUT,
  LOGIN_USER,
  LOGIN_ADMIN,
  AUTH_COOKIES,
  UNAUTHORIZED,
  TODO_CREATED,
  TODO_DELETED,
  NOT_FOUND,
  TODO_UPDATED,
  TODO_SENDED,
};
